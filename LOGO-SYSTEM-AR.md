# نظام اللوغو (Light / Dark) — دليل مختصر

## الفكرة

شعار واحد لا يكفي: النسخة ذات النص الأسود تختفي على خلفية `zinc-950` في الوضع
الداكن، وتختفي دائماً في الشريط الجانبي للوحة التحكم (أسود في الوضعين). لذلك
صار للمطعم ملفان، وقرار «أي ملف يُعرض أين» يعيش في مكان واحد فقط.

| العمود | المحتوى | أين يُستعمل |
| --- | --- | --- |
| `light_logo_url` | نص أسود | الخلفيات الفاتحة، الفواتير المطبوعة، شاشة الكاشير |
| `dark_logo_url` | نص أبيض | الوضع الداكن، الشريط الجانبي للأدمن، شريط السائق |
| `logo_url` | الشعار القديم | احتياطي فقط لمن لم يرفع الملفين بعد |

## 1. قاعدة البيانات

شغّل `public/migration_v17_logo_light_dark.sql` في محرر SQL على Supabase
**قبل** نشر الكود — فـ `GET /api/settings` يقرأ العمودين الجديدين، وسيفشل
الطلب كله إن لم يكونا موجودين.

الملف idempotent (يمكن تشغيله أكثر من مرة)، وينقل تلقائياً `logo_url` الحالي
إلى `light_logo_url` لأن الشعار الحالي هو ذو النص الأسود.

## 2. الإعدادات

`/admin/settings` ← قسم «عام» فيه الآن حقلان بدل واحد. معاينة الشعار الداكن
تظهر على خلفية `zinc-900` كي لا يكون الملف الأبيض غير مرئي في الشاشة نفسها
التي ترفعه منها. رفع الشعار الفاتح يُحدّث `logo_url` القديم معه.

## 3. الاستخدام في الكود

```tsx
import AppLogo from '../components/AppLogo';

<AppLogo />                    // يتبع الثيم المفعّل
<AppLogo variant="dark" />     // الأبيض دائماً — الأشرطة السوداء
<AppLogo variant="light" />    // الأسود دائماً — الطباعة، الكاشير
```

الحجم والشكل يبقيان مسؤولية المكان المستدعي:

```tsx
<AppLogo
  className="flex h-11 w-11 shrink-0 items-center justify-center text-lg"
  fallbackClassName="rounded-xl bg-brand-500"   // يُطبَّق فقط عند غياب أي شعار
/>
```

خارج React (مثل فاتورة الطباعة) تُستعمل الدالة الخالصة مباشرة:

```ts
const logoUrl = resolveLogoSrc(getCachedSettings(), 'light', { variant: 'light' });
```

### صورة خاصة بواجهة معيّنة

أيقونة فئة «Tout» لها صورتها الخاصة (`all_category_image_url`) لكنها تحتاج
التبديل نفسه، فتُمرَّر كمصدر ذي أولوية على الجانب الفاتح:

```tsx
<AppLogo sources={{ light: settings?.all_category_image_url }} … />
```

## 4. سلسلة الاحتياط

الترتيب مصمَّم كي لا يتغيّر أي شيء بصرياً قبل رفع الشعار الأبيض:

- **الجانب الداكن**: `sources.dark` ← `dark_logo_url` ← `sources.light` ←
  `light_logo_url` ← `logo_url`
- **الجانب الفاتح**: `sources.light` ← `light_logo_url` ← `logo_url` ←
  `sources.dark` ← `dark_logo_url`

القيم الفارغة أو المسافات البيضاء تُعامل كـ «غير مضبوطة». وأثناء تحميل
`/api/settings` يحجز المكوّن المساحة دون رسم شيء، تفادياً لوميض العلامة
الخاطئة، ثم يعرض الإيموجي الاحتياطي فقط بعد أن يتأكد أن لا شعار مضبوطاً.

## 5. أين طُبِّق

| الواجهة | النمط |
| --- | --- |
| هيدر قائمة الزبون | `auto` |
| أيقونة «Tout» في قائمة التصنيفات | `auto` + صورة الفئة |
| صفحة التسجيل — لوحة النموذج | `auto` |
| صفحة التسجيل — اللوحة الجانبية (تدرّج داكن دائماً) | `dark` |
| الشريط الجانبي للوحة التحكم + الشريط العلوي للجوال | `dark` |
| الشريط العلوي للسائق | `dark` |
| أيقونة «Tout» في شاشة الكاشير | `light` |
| الفاتورة المطبوعة | `light` |

> ملاحظة: صفحة التسجيل لم تكن تدعم الوضع الداكن إطلاقاً، فأُضيف لها الدعم
> (وزر تبديل الثيم) كي يكون التبديل الديناميكي فيها منطقياً بدل عرض شعار
> أبيض على خلفية بيضاء.
